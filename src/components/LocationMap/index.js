import React from 'react';
import {style} from './index.scss';

const LocationMap = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

LocationMap.propTypes = {

};

export default LocationMap;
