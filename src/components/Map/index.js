import React from 'react';
import {style} from './index.scss';

const Map = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Map.propTypes = {

};

export default Map;
